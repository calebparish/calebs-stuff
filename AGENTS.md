# Agent Rules

- **Never force push.** Use normal `git push origin main` only.
- When adding a new app:
  1. Create a dedicated subdirectory named after the app
  2. Add a link to the app in `index.html` (root project listing)
  3. Add a link to the app in `README.md` (Projects list)
